{% extends 'layout/layout.html.twig' %}

{% block body %}
        
        <section class="container-fluid">
            <div class="article-container-header">Accueil</div>
            <div class="carousel-contain">
                <div class="container-fluid" id="content">
                    <div id="myCarousel" class="carousel slide" data-ride="carousel">
                        
                        
                        <div class="carousel-inner" role="listbox">
                            <div class="item active">
                                <div style="width: 100%; height:300px; margin-left: auto; margin-right: auto; overflow: hidden; display:block; top:0; left: 0; right: 0; bottom: 0; background-image:url('{{ asset('images/event.jpg') }}'); background-repeat: no-repeat; background-size: contain; background-position: center;">
                                    <div class="my-article-container bottom-inside">
                                        <div class="alphabg article-main" style="text-align:center; width:40%">
                                            Inscrivez vous  : <br>
                                            <ul style="list-style-type : none; padding:0">
                                                            {% if valides[0].activite.titre is defined %}
                                                <li><a href="{{ path('act_valide_show', { 'id': valides[0].id }) }}">{{valides[0].activite.titre}}</a></li>
                                                             {% endif %}
                                                             {% if valides[1].activite.titre is defined %}
                                                <li><a href="{{ path('act_valide_show', { 'id': valides[1].id }) }}">{{valides[1].activite.titre}}</a></li>
                                                             {% endif %}
                                                             {% if valides[2].activite.titre is defined %}
                                                <li><a href="{{ path('act_valide_show', { 'id': valides[2].id }) }}">{{valides[2].activite.titre}}</a></li>
                                                            {% endif %}
                                            </ul>
                                        </div>
                                    </div>
                                </div>
                            </div>

                           <div class="item">
                                <div style="width: 100%; height:300px; margin-left: auto; margin-right: auto; overflow: hidden; display:block; top:0; left: 0; right: 0; bottom: 0; background-image:url('{{ asset('images/event.jpg') }}'); background-repeat: no-repeat; background-size: contain; background-position: center;">
                                    <div class="my-article-container bottom-inside">
                                        <div class="alphabg article-main" style="text-align:center; width:40%">
                                            Votez vite pour ces événements : <br>
                                            <ul style="list-style-type : none; padding:0">
                                                        {% if envotes[0].activite.titre is defined %}
                                                <li><a href="{{ path('act_envote_show', { 'id': envotes[0].id }) }}">{{envotes[0].activite.titre}}</a></li>
                                                        {% endif %}
                                                        {% if envotes[1].activite.titre is defined %}
                                                <li><a href="{{ path('act_envote_show', { 'id': envotes[1].id }) }}">{{envotes[1].activite.titre}}</a></li>
                                                {% endif %}
                                                {% if envotes[2].activite.titre is defined %}
                                                <li><a href="{{ path('act_envote_show', { 'id': envotes[2].id }) }}">{{envotes[2].activite.titre}}</a></li>
                                                {% endif %}
                                            </ul>
                                        </div>
                                    </div>
                                </div>
                            </div>

                            <div class="item">
                                <div style="width: 100%; height:300px; margin-left: auto; margin-right: auto; overflow: hidden; display:block; top:0; left: 0; right: 0; bottom: 0; background-image:url('{{ asset('images/event.jpg') }}'); background-repeat: no-repeat; background-size: contain; background-position: center;">
                                    <div class="my-article-container bottom-inside">
                                        <div class="alphabg article-main" style="text-align:center; width:40%">
                                            Nos Events   : <br>
                                            <ul style="list-style-type : none; padding:0">
                                                {% if anciennes[0].activite.titre is defined %}
                                                <li><a href="{{ path('act_ancienne_show', { 'id': anciennes[0].id }) }}">{{anciennes[0].activite.titre}}</a></li>
                                                {% endif %}
                                                {% if anciennes[1].activite.titre is defined %}
                                                <li><a href="{{ path('act_ancienne_show', { 'id': anciennes[1].id }) }}">{{anciennes[1].activite.titre}}</a></li>
                                                {% endif %}
                                                {% if anciennes[2].activite.titre is defined %}
                                                <li><a href="{{ path('act_ancienne_show', { 'id': anciennes[2].id }) }}">{{anciennes[2].activite.titre}}</a></li>
                                                {% endif %}
                                            </ul>
                                        </div>
                                    </div>
                                </div>
                            </div>

                        </div>
 


                        <a class="left carousel-control" href="#myCarousel" role="button" data-slide="prev">
                            <span class="glyphicon glyphicon-chevron-left" aria-hidden="true"></span>
                            <span class="sr-only">Previous</span>
                        </a>
                        <a class="right carousel-control" href="#myCarousel" role="button" data-slide="next">
                            <span class="glyphicon glyphicon-chevron-right" aria-hidden="true"></span>
                            <span class="sr-only">Next</span>
                        </a>
                        
                    </div>
                </div>
            </div>
            <div class="center-inside">
                <div class="my-article-container">
                    <div class="col-xs-12 col-sm-12 col-md-8 col-md-offset-2 col-lg-8 col-lg-offset-2" style="height:auto;">
                        <div class="alphabg">
                    
                            <div class="article-header center-inside" style="width:100%">
                                Tableau de bord
                            </div>
                            
                            <div class="center-inside container-fluid article-main" style="width:100%">
                                <div class="col-md-3" style="height:100%;">
                                    <div class=" center-inside">
                                        <div class="avatar100" style="background-image:url()"></div>
                                    </div>
                                </div>
                                <div class=" col-md-9" style="height:100%;">
                                    <ul>
                                        <li><a href="{{ path('idee_new') }}">Suggérer événement</a></li>
                                        <li><a href=" {{ path('fos_user_profile_show') }}">Gestion de compte</a></li>
                                            {% if is_granted('ROLE_ADMIN') %}
                                         <li><a href=" {{ path('admin_list') }}">Administration</a></li>
                                                   {% endif %}
                                        {% if is_granted('ROLE_CESI') %}
                                         <li><a href=" {{ path('admin_list') }}">Administration</a></li>
                                                   {% endif %}
                                        <li><a href="{{ path('fos_user_security_logout') }}">Déconnexion</a></li>
                                    </ul>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>  
        </section >
    
{% endblock %}
