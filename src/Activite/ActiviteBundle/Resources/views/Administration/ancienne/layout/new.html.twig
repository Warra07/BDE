{% extends 'layout/layout.html.twig' %}

{% block body %}
<div class="center-inside">
         <div id="container">   
        
            <form id="form2" action="{{path('admin_act_ancienne_new')}}" method="post"  enctype="multipart/form-data">  
            
                <h3><span>Ajouter un évènement ancienne</span></h3>
            
                <fieldset><legend>Ajout évènement ancienne</legend>
                    
                  {{form_widget(form)}}
                  {{form_end(form)}}    
                                                
                </fieldset>                 
                        
         </div>
        </div>
{% endblock %}
